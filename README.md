# system-mock-examples
This repository contains sample projects that demonstrate how to mock the `System` class in Java, such as `System.getenv`.

## Project Structure
- **powermock-example** — Example using PowerMock (Java 11)
- **mockito5-example** — Example using Mockito 5 (Java 11)
